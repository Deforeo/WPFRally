<UserControl x:Class="WPFRally.Views.TrackSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource LiveGradient}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="ВЫБЕРИТЕ ТРАССУ" Style="{StaticResource TitleText}" Margin="0,0,0,20"/>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Tracks}"
                 AlternationCount="2"
                 Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding Name}" Width="150" Style="{StaticResource NormalText}"/>
                        <Button Content="Выбрать" 
                                Command="{Binding DataContext.SelectTrackCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                CommandParameter="{Binding}" 
                                Width="150"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="2" Content="Назад" Command="{Binding BackCommand}" Width="150" Margin="10" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>