<UserControl x:Class="WPFRally.Views.CarSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource LiveGradient}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="ВЫБЕРИТЕ АВТОМОБИЛЬ" Style="{StaticResource TitleText}" Margin="0,0,0,20"/>

        <!-- Контейнер с градиентом и fade-out -->
        <Grid Grid.Row="1">
            <ListBox ItemsSource="{Binding Cars}"
         AlternationCount="2"
         Margin="10"
                     >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <TextBlock Text="{Binding Name}" Width="Auto" Style="{StaticResource NormalText}"/>
                            <Button Content="Выбрать" 
                        Command="{Binding DataContext.SelectCarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                        CommandParameter="{Binding}" 
                        Width="150"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Button Grid.Row="2" Content="Назад" Command="{Binding BackCommand}" Width="150" Margin="10" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>